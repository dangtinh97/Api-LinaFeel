<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Upload Key & File</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <style>
      * {
          box-sizing: border-box;
          font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      }

      body {
          background: linear-gradient(135deg, #1f2937, #111827);
          display: flex;
          align-items: center;
          justify-content: center;
          height: 100vh;
          margin: 0;
          padding: 20px;
      }

      .upload-container {
          background: #ffffff10;
          backdrop-filter: blur(14px);
          border: 1px solid #ffffff22;
          border-radius: 16px;
          padding: 40px 35px;
          width: 100%;
          max-width: 400px;
          color: #fff;
          box-shadow: 0 8px 30px rgba(0,0,0,0.4);
          animation: fadeIn 0.4s ease-in-out;
      }

      @keyframes fadeIn {
          from {opacity: 0; transform: translateY(20px);}
          to {opacity: 1; transform: translateY(0);}
      }

      h2 {
          text-align: center;
          margin-bottom: 25px;
          font-weight: 600;
          letter-spacing: 1px;
      }

      .form-group {
          display: flex;
          flex-direction: column;
          margin-bottom: 20px;
      }

      label {
          margin-bottom: 6px;
          font-size: 14px;
          color: #d1d5db;
      }

      input[type="text"],
      input[type="file"] {
          padding: 12px;
          border: none;
          border-radius: 8px;
          background-color: #1f2937;
          color: #fff;
          outline: none;
          font-size: 15px;
          transition: background 0.2s ease;
      }

      input[type="text"]:focus {
          background-color: #111827;
      }

      input::file-selector-button {
          background-color: #3b82f6;
          border: none;
          border-radius: 6px;
          color: white;
          padding: 8px 14px;
          cursor: pointer;
          font-weight: 500;
          transition: background-color 0.25s ease;
      }

      input::file-selector-button:hover {
          background-color: #2563eb;
      }

      button {
          width: 100%;
          padding: 12px;
          background-color: #10b981;
          border: none;
          border-radius: 8px;
          font-size: 16px;
          font-weight: 600;
          color: #fff;
          cursor: pointer;
          transition: background-color 0.25s ease, transform 0.1s ease;
      }

      button:hover {
          background-color: #059669;
      }

      button:active {
          transform: scale(0.98);
      }

      .footer {
          text-align: center;
          font-size: 13px;
          margin-top: 20px;
          color: #9ca3af;
      }

      @media (max-width: 480px) {
          .upload-container {
              padding: 30px 25px;
              border-radius: 12px;
          }

          h2 {
              font-size: 20px;
          }

          input, button {
              font-size: 14px;
              padding: 10px;
          }

          .footer {
              font-size: 12px;
          }
      }
  </style>
</head>
<body>
<div class="upload-container">
  <h2>Upload Key & File</h2>
  <form action="/admin/sync-order" method="post" enctype="multipart/form-data">
    <div class="form-group">
      <label for="orderId">Order ID</label>
      <input type="text" id="orderId" name="order_id" value="{{data.order_id}}" readonly>
    </div>

    <div class="form-group">
      <label for="key">Nhập Key</label>
      <input type="text" id="key" name="key" placeholder="Nhập key..." required>
    </div>

    <div class="form-group">
      <label for="file">Chọn File</label>
      <input type="file" id="file" name="file" required>
    </div>

    <button type="submit">Gửi lên</button>
  </form>
  <div class="footer">
    © 2025 Your Company
  </div>
</div>
</body>
</html>
